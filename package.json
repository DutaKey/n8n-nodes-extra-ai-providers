{
  "name": "n8n-nodes-extra-ai-providers",
  "version": "0.1.0",
  "description": "Custom n8n nodes for various AI Providers (OpenCode, 9Router, etc.)",
  "keywords": [
    "n8n-community-node-package"
  ],
  "author": "DutaKey",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc && mkdir -p dist/nodes/OpenCode && cp nodes/OpenCode/*.svg dist/nodes/OpenCode/",
    "dev": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist"
  ],
  "n8n": {
    "n8nNodesApiVersion": 1,
    "credentials": [
      "dist/credentials/OpenCodeApi.credentials.js"
    ],
    "nodes": [
      "dist/nodes/OpenCode/OpenCode.node.js"
    ]
  },
  "dependencies": {
    "@langchain/core": "^1.1.47",
    "@langchain/openai": "^1.4.6",
    "@n8n/ai-utilities": "^0.15.0",
    "@n8n/n8n-nodes-langchain": "^2.21.2",
    "n8n-core": "^2.16.1",
    "n8n-workflow": "^2.16.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
